# Database
spring.datasource.url=jdbc:mysql://localhost:3306/voting_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Root@123

# Logging (debug while developing)
logging.level.org.springframework.security=DEBUG
logging.level.com.example.voting.security=DEBUG
logging.level.com.example.voting.service=DEBUG

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT settings
# WARNING: Replace this secret with your own secure random 32+ character secret.
jwt.secret=2f8b1f3c9a7d4e6f2b8c9e0a1f6b7c3d9e5a4b1c6d7e8f90

# Expiration in milliseconds (numeric only — no inline comments)
jwt.expiration=3600000
